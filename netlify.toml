[build]
publish = "."

[[headers]]
for = "/*"
[headers.values]
Cache-Control = "no-cache"

# SPA fallback so ?upi_status= and deep links load index.html
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
